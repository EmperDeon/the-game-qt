#include "ModLoader/core/render/mhelper.h"


void MRHelper::drawRect(IVec3 p1, IVec3 p2, bool side, IVec3 c) {
//	glBegin(GL_QUADS);
//	glColor3f(c.x, c.y, c.z);
//
//
//
//	glVertex3f(p1.x, y +  r, z -  r);
//	glVertex3f(p2.x, y +  r, z -  r);
//	glVertex3f(p2.x, y +  r, z +  r);
//	glVertex3f(p1.x, y +  r, z +  r);
//	glEnd();
}
